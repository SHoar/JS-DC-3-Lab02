<div class="row">
    <div class="col-md-4 portfolio-item">
        <a href="#">
            <img class="img-responsive" src={{spotifyImage}} alt="">
        </a>
        <h3>
            <a href="#">{{artist.name}}</a>
        </h3>
        <p>Genre: {{artist.genre}}</p>
        <p>URL: {{artist.spotifyURL}}</p>

    </div>
